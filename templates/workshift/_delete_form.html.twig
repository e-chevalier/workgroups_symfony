<form method="post" action="{{ path('app_workshift_delete', {'id': workshift.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workshift.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
